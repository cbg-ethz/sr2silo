#### Configuration for sr2silo workflow

### Sample Selection
LOCATIONS:
  - 5   # Lugano (TI)
  - 15  # Basel (BS)
  - 17  # Chur (GR)
  - 10  # Zürich (ZH)
  - 16  # Geneva (GE)
  - 25  # Laupen (BE)
START_DATE: "2024-09-15"
END_DATE: "2024-11-15"

### Input Files
BASE_SAMPLE_DIR: "tests/data/samples_large" #"../results"
TIMELINE_FILE: "tests/data/samples_large/timeline_A1_05_2024_10_08.tsv" #"../../work-vp-test/variants/timeline.tsv"
### Output Files
RESULTS_DIR:  "silo-inputs"

### Loculus (to submit results to)
## URLs for Loculus services
KEYCLOAK_TOKEN_URL: "https://authentication-wise-files.loculus.org/realms/loculus/protocol/openid-connect/token"
SUBMISSION_URL: "https://backend-wise-files.loculus.org"

## Authentication credentials
GROUP_ID: 1
# For production use prefer reading them from environment at runtime or prompting the user.
USERNAME: "testuser"
PASSWORD: "testuser"

### LAPIS – hosting the SILO database (to query reference from)
LAPIS_URL: "http://88.198.54.174"

### Subsampling Configuration
ENABLE_SUBSAMPLING: true
SUBSAMPLE_FRACTION: 0.5  # Keep 10% of reads
# Alternative: use SUBSAMPLE_MAX_READS instead of fraction
SUBSAMPLE_MAX_READS: 150  # Keep maximum 100k reads
