# RSV-A sr2silo Configuration

### Virus Metadata
VIRUS_NAME: "rsva"
VIRUS_DISPLAY_NAME: "Respiratory Syncytial Virus A"

### Sample Selection
LOCATIONS:
  - 5   # Lugano (TI)
  - 15  # Basel (BS)
  - 17  # Chur (GR)
  - 10  # Zürich (ZH)
  - 16  # Geneva (GE)
  - 25  # Laupen (BE)

START_DATE: "1_MONTHS_AGO"
END_DATE: "TODAY"

### Input Files
BASE_SAMPLE_DIR: "/cluster/project/pangolin/processes/rsv/RSVA/vpipe_output"
TIMELINE_FILE: "/cluster/project/pangolin/processes/rsv/RSVA/working/timeline.tsv"

### Output Files
RESULTS_DIR: "../../silo-inputs/rsva"
LOG_DIR: "../../logs/rsva"

### Loculus Configuration
KEYCLOAK_TOKEN_URL: "https://auth.db.wasap.genspectrum.org/realms/loculus/protocol/openid-connect/token"
BACKEND_URL: "https://api.db.wasap.genspectrum.org/backend"
GROUP_ID: 1
ORGANISM: "rsva"
# LAPIS_URL:

# Auto-release: automatically approve sequences after submission
AUTO_RELEASE: true
RELEASE_DELAY: 180  # Seconds to wait before releasing

# Credentials loaded from secrets/credentials.enc at runtime via decrypt.py
# Do NOT include plaintext USERNAME or PASSWORD here

### Subsampling Configuration
ENABLE_SUBSAMPLING: true
SUBSAMPLE_MAX_READS: 4500000

### Reference Filtering
# The reference accession to filter reads by when processing BAM files.
# This should match the @SQ SN field in the BAM header.
# Find this by running: samtools view -H REF_aln_trim.bam | grep @SQ
# Leave empty or omit to process all reads (backward compatible).
REFERENCE_ACCESSION: "EPI_ISL_412866"  # RSV-A reference

### Resource Configuration
SLURM_CPUS: 4
SLURM_MEM: "16G"
